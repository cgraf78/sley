#!/usr/bin/env bash
# pre-applypatch — gate commits created by `git am` through Sley.

set -u

_hook_dir=$(cd -P -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd) || exit 0
_gate=${SLEY_GIT_COMMIT_GATE:-"$_hook_dir/sley-commit-gate"}
exec "$_gate"
